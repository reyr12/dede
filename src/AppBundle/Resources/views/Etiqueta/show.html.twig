{% extends '::base.html.twig' %}
{% block title %}.::Etiqueta::.!{% endblock %}
{% block contenido -%}
     <div class="row wrapper border-bottom white-bg page-heading">
                <div class="col-lg-10">
                    <h2>Etiqueta</h2>
                    <ol class="breadcrumb">
                        <li>
                            <a href="{{ path('homepage') }}">Home</a>
                        </li>
                        <li>
                            <a>Detalle</a>
                        </li>
                        <li class="active">
                            <strong>Etiqueta</strong>
                        </li>
                    </ol>
                </div>
                <div class="col-lg-2">

                </div>
            </div>
 <div class="wrapper wrapper-content animated fadeInRight">
            <div class="row">
                <div class="col-lg-12">
                <div class="ibox float-e-margins">
                    <div class="ibox-title">
                        <h5>Detalle de Etiqueta</h5>
                          <a href="{{ path('administrar_etiqueta') }}" style="margin-left: 70px;" class="btn btn-danger btn-circle" title="Regresar al listado">
                                 <i class="fa fa-arrow-circle-o-left"></i>
                             </a>    
                        <div class="ibox-tools">                         
                            <a class="collapse-link">
                                <i class="fa fa-chevron-up"></i>
                            </a>
                                                     
                            <a class="close-link">
                                <i class="fa fa-times"></i>
                            </a>
                        </div>
                    </div>
                    <div class="ibox-content">

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Titulo</th>
                <td>{{ entity.titulo }}</td>
            </tr>
            <tr>
                <th>Clase</th>
                <td>{{ entity.clase }}</td>
            </tr>
            <tr>
                <th>Precioanterior</th>
                <td>{{ entity.precioAnterior }}</td>
            </tr>
        </tbody>
    </table>


    <a href="{{ path('administrar_etiqueta') }}" class="btn btn-w-m btn-warning"><i class="fa fa-warning"></i> Cancelar</a>
      <a href="{{ path('administrar_modelo_edit', { 'id': entity.id }) }}" class="btn btn-primary"><i class="fa fa-edit"></i> Editar</a>
       {{ form_start(delete_form) }}
                        <button type="submit" class="btn btn-w-m btn-danger" data-original-title="Eliminar transaccion"><i class="fa fa-minus-square"></i> Eliminar</button>
                    {{ form_end(delete_form) }}
{% endblock %}
 </div>
            </div>
            </div>
            </div>

        
    {% endblock %}